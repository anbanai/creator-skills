{
  "description": "Anban 智能创作助手 plugin lifecycle hooks for agent delivery summaries and quality verification",
  "hooks": {
    "SubagentStop": [
      {
        "matcher": "^anban:seednote$",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/seednote-quality-gate.sh",
            "args": [],
            "timeout": 30
          }
        ]
      }
    ]
  }
}
